# Image du frontal local. Elle n'existe qu'ici : en production, ce rôle est
# tenu par le tunnel Cloudflare et par Dokploy, qui ne se construisent pas.
FROM caddy:2-alpine

COPY Caddyfile /etc/caddy/Caddyfile
