import type { EtatMedia, EvenementStocke } from '@/domain/types'

/**
 * Squelette : le contrat est posé et testé, l'implémentation arrive au
 * commit suivant.
 */
export function statutCourant(_evenements: readonly EvenementStocke[]): EtatMedia {
  throw new Error('Pas encore implémenté')
}
