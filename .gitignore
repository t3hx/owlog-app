node_modules/
dist/
dist-ssr/
*.local
.DS_Store
.env
.env.*
!.env.example
coverage/
.vite/
*.tsbuildinfo
.gstack/

# Reglages locaux de Claude Code : propres a la machine, jamais partages.
.claude/settings.local.json

# Dump GDPR TVTime personnel (tokens, hashs, IP) — ne jamais commiter
docs/tvtime-gdpr-data/

# Artefacts de QA Playwright (captures, logs de console). Sortie d outil,
# jamais du code — et un log de session peut contenir un jeton de connexion.
.playwright-mcp/
